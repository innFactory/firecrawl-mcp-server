name: Deploy Images to GHCR staging

env:
  DOTNET_VERSION: '6.0.x'

# innFactory fork: der push-Trigger ist entfernt. Dieser Workflow gehoert
# upstream und wuerde nach ghcr.io/firecrawl/... pushen — in diesem Fork
# scheitert er bei jedem Push auf main mit permission_denied und verdeckt
# damit echte Fehlschlaege. Nur noch manuell ausloesbar.
on:
  workflow_dispatch:


jobs:
  push-image:
    runs-on: ubuntu-latest
    steps:

      - name: 'Checkout GitHub Action'
        uses: actions/checkout@main

      - name: 'Login to GitHub Container Registry'
        uses: docker/login-action@v1
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: 'Set up Docker Buildx'
        uses: docker/setup-buildx-action@v1

      - name: 'Build Service Image'
        uses: docker/build-push-action@v2
        with:
          context: .
          file: ./Dockerfile.service
          push: true
          tags: ghcr.io/firecrawl/firecrawl-mcp-server-alpha:latest
          cache-from: type=registry,ref=ghcr.io/firecrawl/firecrawl-mcp-server-alpha:latest
          cache-to: type=inline
